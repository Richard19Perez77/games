// import { device, element, by, expect } from "detox";

// describe("Home Screen", () => {
//   beforeAll(async () => {
//     await device.launchApp();
//   });

//   it("should display 'Index' at the top of the page", async () => {
//     await expect(element(by.text("Index"))).toBeVisible();
//   });
// });
